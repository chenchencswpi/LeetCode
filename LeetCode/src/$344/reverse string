Write a function that takes a string as input and returns the string reversed.

Example:
Given s = "hello", return "olleh".

写一个方法来反转数组。

由于字符串是immutable的，那么我们刚开始需要将字符串放在数组里进行操作。然后用双指针（一头一尾）开始进行调换操作。

//插入方法reverseString()