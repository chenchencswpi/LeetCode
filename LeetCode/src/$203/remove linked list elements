Remove all elements from a linked list of integers that have value val.

Example
Given: 1 --> 2 --> 6 --> 3 --> 4 --> 5 --> 6, val = 6
Return: 1 --> 2 --> 3 --> 4 --> 5

给我们一个单向链表，让我们移除所有等于目标值val的元素。

一般做这种题的时候，摆渡翁建议大家这么做：已经有一段链表，它们都不等于val。然后判断这段链表的下一个节点x，是不是等于val。如果不是，那么欢迎你
加入我们的大家庭。如果是，那对不起了，直接将这段链表的最后一位指向x的下一位，将x从链表移除。那么问题来了，怎么初始化这一段链表呢？其实在做这些
链表类型的题目时候，一个很常用的方法就是新加一个链表头，然后指向旧的链表头，这样做是以防一开始旧的链表头就不符合要求。好了，大家看代码慢慢体会：

//插入方法removeElements()